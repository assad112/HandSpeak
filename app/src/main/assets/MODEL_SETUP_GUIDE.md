# 🔧 دليل إعداد النماذج - HandSpeak

## ✅ الملفات المكتملة

### 1. ✅ نموذج MediaPipe (hand_landmarker.task)
- **الحالة**: ✅ تم التحميل بنجاح
- **الحجم**: ~7.8 MB
- **الموقع**: `app/src/main/assets/hand_landmarker.task`
- **الوظيفة**: التعرف على نقاط اليد (21 landmark)

## ⚠️ الملفات المطلوبة

### 2. ❌ نموذج TensorFlow Lite (arabic_sign_lstm.tflite)

**الحالة الحالية**: غير موجود - يحتاج تدريب

#### الطريقة الأولى: استخدام نموذج تجريبي (للاختبار فقط)

يمكنك إنشاء نموذج تجريبي بسيط للاختبار باستخدام Python:

```python
import tensorflow as tf
import numpy as np

# إنشاء نموذج بسيط للاختبار
model = tf.keras.Sequential([
    tf.keras.layers.InputLayer(input_shape=(63,)),  # 21 landmarks × 3
    tf.keras.layers.Dense(128, activation='relu'),
    tf.keras.layers.Dropout(0.3),
    tf.keras.layers.Dense(64, activation='relu'),
    tf.keras.layers.Dense(38, activation='softmax')  # 38 تصنيف (28 حرف + 10 كلمات)
])

# تجميع النموذج
model.compile(
    optimizer='adam',
    loss='categorical_crossentropy',
    metrics=['accuracy']
)

# تحويل إلى TFLite
converter = tf.lite.TFLiteConverter.from_keras_model(model)
converter.optimizations = [tf.lite.Optimize.DEFAULT]
tflite_model = converter.convert()

# حفظ
with open('arabic_sign_lstm.tflite', 'wb') as f:
    f.write(tflite_model)

print("✅ تم إنشاء النموذج التجريبي!")
```

**ملاحظة**: هذا النموذج التجريبي لن يعطي نتائج دقيقة، لكنه يسمح باختبار التطبيق.

#### الطريقة الثانية: تدريب نموذج حقيقي (موصى به)

راجع ملف `SETUP_MODELS.md` للخطوات الكاملة لتدريب النموذج على Google Colab.

**المتطلبات:**
1. قاعدة بيانات RGB_ArSL_dataset
2. Google Colab أو بيئة Python مع TensorFlow
3. MediaPipe لاستخراج landmarks
4. وقت تدريب: 1-3 ساعات حسب حجم البيانات

### 3. 📁 صور الإشارات (signs/)

**الحالة الحالية**: ✅ تم إنشاء هيكل المجلدات

**المطلوب**: إضافة الصور الفعلية في كل مجلد

#### هيكل المجلدات الموجود:
```
app/src/main/assets/signs/
├── alef/          ✅ موجود
├── baa/           ✅ موجود
├── taa/           ✅ موجود
├── ...            (28 حرف)
├── marhaba/       ✅ موجود
├── shokran/       ✅ موجود
└── ...            (10 كلمات)
```

#### كيفية إضافة الصور:

1. **للحروف (28 حرف)**:
   - انسخ 3-10 صور لكل حرف
   - ضعها في المجلد المناسب: `signs/alef/`, `signs/baa/`, إلخ
   - اسم الملفات: `1.png`, `2.png`, `3.png`, ...

2. **للكلمات (10 كلمات)**:
   - انسخ 3-10 صور لكل كلمة
   - ضعها في المجلد المناسب: `signs/marhaba/`, `signs/shokran/`, إلخ

**مصادر الصور:**
- قاعدة بيانات RGB_ArSL_dataset
- تصوير يدوي (استخدم خلفية موحدة)
- مواقع متخصصة في لغة الإشارة العربية

## 📋 قائمة التحقق

استخدم هذه القائمة للتحقق من اكتمال الإعداد:

### النماذج
- [x] ✅ hand_landmarker.task (تم التحميل)
- [ ] ❌ arabic_sign_lstm.tflite (يحتاج تدريب)

### الملفات المساعدة
- [x] ✅ labels.json (موجود)
- [x] ✅ sign_map.json (موجود)

### الصور
- [x] ✅ هيكل المجلدات (تم الإنشاء)
- [ ] ❌ صور الحروف (يحتاج إضافة)
- [ ] ❌ صور الكلمات (يحتاج إضافة)

## 🚀 خطوات التشغيل السريع

### 1. إضافة نموذج TFLite

**خيار أ**: استخدام نموذج تجريبي (للاختبار)
```bash
# شغّل الكود Python أعلاه
# ثم انسخ الملف:
cp arabic_sign_lstm.tflite app/src/main/assets/
```

**خيار ب**: تدريب نموذج حقيقي
- اتبع التعليمات في `SETUP_MODELS.md`
- بعد التدريب، انسخ الملف إلى `app/src/main/assets/`

### 2. إضافة صور الإشارات

```bash
# مثال: إضافة صور لحرف أ
# انسخ الصور إلى:
app/src/main/assets/signs/alef/
#   ├── 1.png
#   ├── 2.png
#   ├── 3.png
#   └── ...
```

### 3. Build و Test

```bash
# Clean و Rebuild
./gradlew clean
./gradlew assembleDebug

# تشغيل على الجهاز
./gradlew installDebug
```

## 🔍 التحقق من الملفات

بعد إضافة جميع الملفات، تحقق من وجودها:

```bash
# في PowerShell
Get-ChildItem app\src\main\assets\ -Recurse | Select-Object FullName

# يجب أن ترى:
# - hand_landmarker.task ✅
# - arabic_sign_lstm.tflite (بعد إضافته)
# - labels.json ✅
# - sign_map.json ✅
# - signs/ (مع الصور) ✅
```

## 📊 حالة الملفات الحالية

| الملف | الحالة | الحجم | الوصف |
|------|--------|-------|-------|
| hand_landmarker.task | ✅ موجود | ~7.8 MB | نموذج MediaPipe |
| arabic_sign_lstm.tflite | ❌ مفقود | - | نموذج TFLite |
| labels.json | ✅ موجود | 331 B | قائمة التصنيفات |
| sign_map.json | ✅ موجود | 2.7 KB | خريطة الإشارات |
| signs/ | ✅ موجود | - | مجلد الصور (فارغ) |

## 💡 نصائح

1. **ابدأ بالنموذج التجريبي**: استخدم النموذج التجريبي أولاً لاختبار التطبيق
2. **أضف الصور تدريجياً**: لا حاجة لإضافة كل الصور دفعة واحدة، ابدأ بعدة إشارات
3. **اختبر بعد كل خطوة**: بعد إضافة كل ملف، اختبر التطبيق للتأكد من عمله
4. **استخدم Logcat**: راقب Logcat لمعرفة أي أخطاء في تحميل الملفات

## 🆘 حل المشاكل

### خطأ: "Model file not found"
- تأكد من وجود `arabic_sign_lstm.tflite` في `app/src/main/assets/`
- قم بـ Clean و Rebuild

### خطأ: "Failed to create HandLandmarker"
- تأكد من وجود `hand_landmarker.task` ✅ (موجود)
- تحقق من اسم الملف (يجب أن يكون بالضبط `hand_landmarker.task`)

### الصور لا تظهر
- تأكد من صحة أسماء المجلدات في `sign_map.json`
- تحقق من وجود الصور في المسار الصحيح
- تأكد من تسمية الصور: `1.png`, `2.png`, إلخ

## 📚 موارد إضافية

- [SETUP_MODELS.md](SETUP_MODELS.md) - دليل شامل لتدريب النماذج
- [signs/README.md](signs/README.md) - دليل إضافة الصور
- [MediaPipe Documentation](https://developers.google.com/mediapipe)
- [TensorFlow Lite Guide](https://www.tensorflow.org/lite)

---

**آخر تحديث**: نوفمبر 2025  
**الحالة**: جاهز للإعداد ✅

