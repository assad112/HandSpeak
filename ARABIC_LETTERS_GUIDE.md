# 🔤 دليل سريع: التعرف على الحروف العربية

## 📊 الوضع الحالي

### ✅ ما يعمل:
- النموذج موجود: `arabic_sign_lstm.tflite`
- الحروف موجودة: `labels.json` (28 حرف عربي)
- النظام يجمع 5 إطارات ثابتة
- عتبة الثقة: **20%** (للاختبار)

### ⚠️ المشكلة:
النموذج الحالي **غير مدرب جيداً** على الحروف العربية، لذلك:
- قد لا يتعرف على الحروف
- الثقة منخفضة جداً
- النتائج غير دقيقة

---

## 🎯 الحل: تدريب نموذج جديد

### الطريقة الأولى: استخدام نموذج مُدرَّب مسبقاً

ابحث عن نموذج مدرب على **Arabic Sign Language** واستبدله:

```
app/src/main/assets/arabic_sign_lstm.tflite
```

مصادر مقترحة:
- Kaggle: https://www.kaggle.com/datasets/arabic-sign-language
- GitHub: ابحث عن "Arabic Sign Language Dataset"
- ArSL Dataset: https://www.example.com/arsl

### الطريقة الثانية: تدريب نموذج جديد

#### 1. تحضير البيانات

احتجت:
- **صور** أو **landmarks** للحروف العربية (28 حرف)
- **100-500 عينة** لكل حرف
- **أشخاص مختلفين** لتنوع البيانات

#### 2. تشغيل السكريبت

```bash
cd scripts
python train_arabic_letters_model.py
```

**ملاحظة:** السكريبت الحالي يستخدم بيانات **تجريبية**. يجب استبدالها ببيانات حقيقية!

#### 3. تعديل السكريبت

في `train_arabic_letters_model.py`، استبدل:

```python
def generate_sample_data(num_samples=100):
    # ❌ حالياً: بيانات عشوائية
    sequence = np.random.rand(SEQUENCE_LENGTH, INPUT_SIZE)
    
    # ✅ يجب أن تكون: بيانات حقيقية
    # مثال: تحميل من مجلد
    for image_path in glob.glob(f"dataset/{letter}/*.jpg"):
        landmarks = extract_landmarks(image_path)
        sequence = normalize_landmarks(landmarks)
```

---

## 🔧 تحسينات مؤقتة

### 1. خفضنا عتبة الثقة

من **50%** إلى **20%** للاختبار:

```kotlin
val minConfidence = 0.20f  // 20%
```

### 2. إضافة logs تفصيلية

الآن يطبع:
- أعلى 5 تنبؤات مع نسبها
- سبب عدم القبول (ثقة منخفضة)

```
LSTM Top 5 predictions: أ (25%), ب (18%), ت (15%), ث (12%), ج (10%)
```

### 3. عرض النتائج الضعيفة

حتى لو الثقة منخفضة، راح يظهر:

```
⚠️ ثقة منخفضة: أ (15%)
```

---

## 🧪 اختبار النموذج الحالي

### 1. ثبّت التطبيق

```powershell
adb install -r app\build\outputs\apk\release\app-release.apk
```

### 2. شاهد Logs

```powershell
adb logcat -s SignToTextViewModel:D SignLanguageClassifier:D
```

### 3. جرّب التطبيق

1. افتح "ترجمة إشارة إلى نص"
2. ثبّت يدك على شكل حرف عربي
3. انتظر 5 إطارات
4. راقب:
   - Top 5 predictions في Logcat
   - النتيجة على الشاشة
   - نسبة الثقة

---

## 📈 ماذا تتوقع؟

### مع النموذج الحالي (بيانات تجريبية):

❌ **لن يعمل جيداً!**
- الحروف عشوائية
- الثقة منخفضة (< 20%)
- غير دقيق

### مع نموذج مُدرَّب على بيانات حقيقية:

✅ **سيعمل بكفاءة!**
- التعرف على الحروف العربية
- الثقة عالية (> 70%)
- دقة جيدة

---

## 🎓 مصادر البيانات

### 1. ArSL2018 Dataset
- 54,049 صورة
- 32 صف (28 حرف + 4 كلمات)
- مصدر: KSU (King Saud University)

### 2. AHSLI Dataset  
- 14,400 صورة
- 28 حرف عربي
- مصدر: Kaggle

### 3. جمع بيانات خاصة
- استخدم الكاميرا
- سجّل landmarks لكل حرف
- احفظ في JSON

---

## 🔄 الخطوات القادمة

### خيار 1: استخدام نموذج جاهز (الأسرع)

1. ابحث عن نموذج ArSL مُدرَّب
2. نزّله بصيغة `.tflite`
3. استبدل `arabic_sign_lstm.tflite`
4. اختبر

### خيار 2: تدريب نموذج (الأدق)

1. جمّع dataset للحروف العربية
2. عدّل `train_arabic_letters_model.py`
3. درّب النموذج
4. انسخ إلى `assets/`

### خيار 3: استخدام Dense بدل LSTM (للحروف الثابتة)

الحروف الثابتة لا تحتاج LSTM (sequences). يمكن استخدام **Dense NN**:

```kotlin
val useLSTM = false  // في الإعدادات
```

ميزات Dense:
- ✅ أسرع
- ✅ أصغر حجماً
- ✅ أنسب للحروف الثابتة
- ❌ لا يدعم الحركات

---

## 📞 الدعم

إذا تحتاج مساعدة:

1. شارك **Logcat output**
2. حدد **الحرف** اللي تجربه
3. أرسل **نسبة الثقة** اللي تظهر

---

## ✅ ملخص التعديلات

| التعديل | القيمة القديمة | القيمة الجديدة |
|---------|----------------|----------------|
| عتبة الثقة | 50% | **20%** |
| عدد الإطارات | 20 | **5** |
| حد الحركة | 2% | **15%** |
| إطارات ثابتة | 2 | **3** |
| وقت الكشف | 2 ثانية | **1.5 ثانية** |
| عرض Top 5 | ❌ | ✅ |
| عرض الثقة المنخفضة | ❌ | ✅ |

---

**📅 آخر تحديث:** ديسمبر 2025
