# 📥 كيفية تحميل الصور في HandSpeak

**تاريخ التحديث**: نوفمبر 2025

---

## 🎯 الطرق المتاحة لتحميل الصور

### الطريقة 1: استخدام التطبيق (الأسهل) ⭐

#### الخطوات:

1. **افتح التطبيق**
   ```
   HandSpeak → الإعدادات
   ```

2. **اذهب إلى قسم الصور**
   ```
   الإعدادات → الصور → تحميل الصور
   ```

3. **املأ النموذج:**
   - **رابط الصورة (URL)**: 
     ```
     مثال: https://example.com/signs/alef/1.png
     ```
   - **اسم المجلد**: 
     ```
     مثال: alef, marhaba, baa, shokran
     ```
   - **رقم الصورة**: 
     ```
     1, 2, 3, 4, 5, ...
     ```

4. **اضغط "تحميل الصورة"**
   - انتظر حتى يكتمل التحميل
   - ستظهر رسالة نجاح ✅ أو خطأ ❌

---

### الطريقة 2: استخدام السكريبتات Python

#### أ. إنشاء صور Placeholder (تم بالفعل ✅)

```bash
cd scripts
python create_placeholder_images.py
```

**النتيجة:**
- ✅ 190 صورة placeholder جاهزة
- ✅ في `app/src/main/assets/signs/`

---

#### ب. تحميل من Google Drive

**الخطوة 1: تثبيت المكتبات**
```bash
pip install gdown requests pillow tqdm
```

**الخطوة 2: تحميل المجلد**
```bash
python scripts/download_images_from_drive.py --folder-id YOUR_FOLDER_ID
```

**مثال:**
```bash
python scripts/download_images_from_drive.py --folder-id 1ABC123xyz789
```

---

#### ج. تحميل من URLs (ملف JSON)

**إنشاء ملف `image_urls.json`:**
```json
{
  "alef": [
    "https://example.com/signs/alef/1.png",
    "https://example.com/signs/alef/2.png",
    "https://example.com/signs/alef/3.png"
  ],
  "marhaba": [
    "https://example.com/signs/marhaba/1.png",
    "https://example.com/signs/marhaba/2.png"
  ]
}
```

**تشغيل السكريبت:**
```bash
python scripts/download_sign_images.py --config image_urls.json
```

---

### الطريقة 3: النسخ اليدوي

#### الخطوات:

1. **احصل على الصور**
   - من قاعدة بيانات RGB_ArSL_dataset
   - من Google Drive
   - من مواقع أخرى

2. **انسخ الصور إلى:**
   ```
   app/src/main/assets/signs/
   ├── alef/
   │   ├── 1.png
   │   ├── 2.png
   │   └── ...
   ├── marhaba/
   │   └── ...
   └── ...
   ```

3. **تأكد من:**
   - ✅ التنسيق: PNG أو JPG
   - ✅ التسمية: 1.png, 2.png, 3.png, ...
   - ✅ الحجم: 512×512 أو أكبر

---

## 📋 مثال تفصيلي: تحميل صور حرف "أ"

### في التطبيق:

1. **افتح التطبيق**
   ```
   HandSpeak
   ```

2. **اذهب إلى الإعدادات**
   ```
   الإعدادات (Settings)
   ```

3. **اضغط على "تحميل الصور"**
   ```
   الصور → تحميل الصور
   ```

4. **حمّل الصورة الأولى:**
   - **URL**: `https://example.com/signs/alef/1.png`
   - **المجلد**: `alef`
   - **الرقم**: `1`
   - اضغط **"تحميل الصورة"**

5. **كرر للصور الأخرى:**
   - الصورة 2: URL → `alef` → `2`
   - الصورة 3: URL → `alef` → `3`
   - وهكذا...

---

## 🔧 كيف يعمل النظام

### 1. التحميل من الإنترنت

```kotlin
// في ImageDownloader.kt
suspend fun downloadImage(
    context: Context,
    imageUrl: String,    // رابط الصورة
    folder: String,       // اسم المجلد
    index: Int           // رقم الصورة
): Bitmap?
```

**العملية:**
1. ✅ الاتصال بالـ URL
2. ✅ تحميل الصورة
3. ✅ حفظها في Internal Storage
4. ✅ إرجاع Bitmap

---

### 2. التخزين

**الموقع:**
```
/data/data/com.example.handspeak/files/sign_images/
├── alef/
│   ├── 1.png
│   ├── 2.png
│   └── ...
└── ...
```

**الوصول:**
- ✅ خاص بالتطبيق فقط
- ✅ آمن ومحمي
- ✅ لا يحتاج صلاحيات

---

### 3. البحث والاستخدام

**أولوية البحث:**
```
1. Storage المحلي (الصور المحمّلة) ← أولوية
   ↓ (إذا لم توجد)
2. Assets (الصور المدمجة) ← احتياطي
```

**في ImageHelper.kt:**
```kotlin
fun getImagePath(context: Context, folder: String, index: Int): String {
    // أولاً: البحث في Storage المحلي
    val localPath = ImageDownloader.getLocalImagePath(context, folder, index)
    if (localPath != null) {
        return localPath  // ✅ استخدم الصورة المحمّلة
    }
    
    // ثانياً: استخدام Assets
    return "file:///android_asset/signs/$folder/$index.png"
}
```

---

## 💡 نصائح مهمة

### 1. أسماء المجلدات
- ✅ استخدم الأسماء من `sign_map.json`
- ✅ يجب أن تطابق تماماً (حساسة لحالة الأحرف)
- ✅ أمثلة: `alef`, `marhaba`, `baa`, `shokran`

### 2. تنسيق الصور
- ✅ **PNG** (مُفضّل) أو **JPG/JPEG**
- ✅ **الحجم**: 512×512 بكسل أو أكبر
- ✅ **الجودة**: واضحة وضوح الشمس

### 3. ترتيب الصور
- ✅ ابدأ من `1.png`
- ✅ استمر بشكل متسلسل: `2.png`, `3.png`, ...
- ✅ لا تترك فجوات في الأرقام

### 4. عدد الصور
- ✅ **الحد الأدنى**: 3 صور لكل إشارة
- ✅ **الموصى به**: 5-10 صور
- ✅ **لـ Animation سلس**: 8-12 صورة

---

## 🎨 مثال كامل: تحميل صور كلمة "مرحبا"

### في التطبيق:

```
1. افتح: الإعدادات → الصور → تحميل الصور

2. الصورة 1:
   URL: https://example.com/signs/marhaba/1.png
   المجلد: marhaba
   الرقم: 1
   → اضغط "تحميل"

3. الصورة 2:
   URL: https://example.com/signs/marhaba/2.png
   المجلد: marhaba
   الرقم: 2
   → اضغط "تحميل"

4. كرر للصور 3, 4, 5...
```

---

## 📊 إدارة الصور المحمّلة

### عرض حجم Cache:
- في شاشة التحميل، ستجد **"معلومات التخزين"**
- يعرض حجم جميع الصور المحفوظة بالـ MB

### تنظيف Cache:
- اضغط على **"تنظيف Cache"**
- سيتم حذف جميع الصور المحمّلة
- الصور في Assets تبقى كما هي

---

## ⚠️ حل المشاكل

### المشكلة 1: فشل التحميل
**السبب:**
- ❌ رابط غير صحيح
- ❌ لا يوجد اتصال بالإنترنت
- ❌ الصورة غير موجودة

**الحل:**
- ✅ تحقق من الرابط
- ✅ تأكد من الاتصال بالإنترنت
- ✅ جرب رابط آخر

---

### المشكلة 2: الصورة لا تظهر
**السبب:**
- ❌ اسم المجلد خاطئ
- ❌ رقم الصورة خاطئ
- ❌ الصورة لم تُحمّل بنجاح

**الحل:**
- ✅ تحقق من اسم المجلد في `sign_map.json`
- ✅ تأكد من الترقيم المتسلسل
- ✅ تحقق من رسالة النجاح

---

### المشكلة 3: Cache ممتلئ
**السبب:**
- ❌ تجاوز الحد الأقصى (50 MB)

**الحل:**
- ✅ اضغط "تنظيف Cache"
- ✅ أو احذف الصور القديمة يدوياً

---

## ✅ الخلاصة

### الطرق المتاحة:
1. ✅ **في التطبيق** - الأسهل والأسرع
2. ✅ **السكريبتات Python** - للتحميل الجماعي
3. ✅ **النسخ اليدوي** - للتحكم الكامل

### الخطوات الأساسية:
1. ✅ احصل على رابط الصورة (URL)
2. ✅ حدد اسم المجلد
3. ✅ حدد رقم الصورة
4. ✅ حمّل من التطبيق أو استخدم سكريبت

---

**آخر تحديث**: نوفمبر 2025  
**الحالة**: ✅ **جاهز للاستخدام**

