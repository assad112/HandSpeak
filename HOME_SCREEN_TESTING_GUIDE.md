# 🧪 دليل اختبار التصميم الجديد - صفحة الهوم

**تاريخ الإنشاء:** ديسمبر 2025

---

## 📋 فهرس الاختبارات

1. [الاختبارات الأساسية](#الاختبارات-الأساسية)
2. [اختبار التنقل](#اختبار-التنقل)
3. [اختبار Dark Mode](#اختبار-dark-mode)
4. [اختبار المصادقة](#اختبار-المصادقة)
5. [اختبارات الأداء](#اختبارات-الأداء)

---

## ✅ الاختبارات الأساسية

### 1️⃣ فتح الصفحة
**الخطوات:**
1. شغّل التطبيق
2. سجّل الدخول إذا كان ضرورياً
3. انتظر حتى تظهر صفحة الهوم

**النتائج المتوقعة:**
- ✅ الصفحة تظهر بسلاسة
- ✅ جميع الخيارات الأربعة تظهر
- ✅ الشريط السفلي يظهر بـ 5 أيقونات
- ✅ بدون أخطاء أو تعليقات

```
Expected Output:
┌─────────────────────────────┐
│                             │
│  Account        [👤]        │
│  ☐ DarkMod      [🌙]        │
│  Favorite       [❤️]        │
│  Log out        [↪️]        │
│                             │
└─────────────────────────────┘
│ 🏠  📚  📷  🎤  📝          │
```

### 2️⃣ تحميل العناصر
**الخطوات:**
1. راقب محمّل الذاكرة (Memory Profiler)
2. لاحظ وقت التحميل

**النتائج المتوقعة:**
- ✅ وقت التحميل < 500ms
- ✅ استهلاك الذاكرة < 50MB إضافي
- ✅ لا توجد تسريبات ذاكرة

```kotlin
// في Logcat تتوقع:
MainActivity: setContent called
MainActivity: NavController created
MainActivity: NavGraph created
```

---

## 🔄 اختبار التنقل

### 1️⃣ الانتقال إلى Account
**الخطوات:**
1. اضغط على "Account"

**النتائج المتوقعة:**
- ✅ الانتقال إلى شاشة Login
- ✅ بدون أخطاء

**الكود:**
```kotlin
Card(
    modifier = Modifier.clickable { 
        navController.navigate(Screen.Login.route) 
    }
)
```

### 2️⃣ الانتقال إلى History
**الخطوات:**
1. اضغط على "Favorite"

**النتائج المتوقعة:**
- ✅ الانتقال إلى شاشة History
- ✅ عرض السجل السابق

**الكود:**
```kotlin
Card(
    modifier = Modifier.clickable { 
        navController.navigate(Screen.History.route) 
    }
)
```

### 3️⃣ الانتقال عبر الشريط السفلي
**الخطوات:**
1. اضغط على "📚 Learn"
2. اضغط على "📷 Camera"
3. اضغط على "🎤 Voice"
4. اضغط على "📝 Text"
5. اضغط على "🏠 Home" للعودة

**النتائج المتوقعة:**
- ✅ تنقل سلس بين الشاشات
- ✅ Home يبقى محدد عند الاختيار
- ✅ لا توجد مشاكل في الانتقالات

```kotlin
NavigationBarItem(
    selected = true,  // عند Home
    onClick = { /* stay */ }
)

NavigationBarItem(
    selected = false,
    onClick = { navController.navigate(Screen.Learn.route) }
)
```

---

## 🌙 اختبار Dark Mode

### 1️⃣ تفعيل Dark Mode
**الخطوات:**
1. اضغط على Switch في "DarkMod"
2. لاحظ التغييرات الفورية

**النتائج المتوقعة:**
- ✅ الخلفية تتغير إلى داكنة
- ✅ الألوان تتكيف
- ✅ النص يبقى واضحاً
- ✅ التغيير فوري بدون تأخير

**الكود:**
```kotlin
Switch(
    checked = settingsState.isDarkMode,
    onCheckedChange = { 
        settingsViewModel.setDarkMode(it)  // ✅ تحديث فوري
    }
)
```

### 2️⃣ التغيير يستمر عند الرجوع
**الخطوات:**
1. فعّل Dark Mode
2. انتقل إلى شاشة أخرى
3. عد للهوم
4. تحقق من أن Dark Mode لا يزال مفعلاً

**النتائج المتوقعة:**
- ✅ الحالة محفوظة
- ✅ Dark Mode يبقى مفعلاً

### 3️⃣ الألوان الديناميكية
**الخطوات:**
1. في Light Mode: لاحظ الألوان الفاتحة
2. في Dark Mode: لاحظ الألوان الداكنة

**النتائج المتوقعة:**
- ✅ جميع العناصر تغير ألوانها
- ✅ النص واضح في كلا الوضعين
- ✅ "Log out" يبقى أحمر في الوضعين

```kotlin
// Light Mode
Text(
    "Log out",
    color = Color.Red  // ✅ يبقى أحمر دائماً
)

// Dark Mode
Text(
    "Log out",
    color = Color.Red  // ✅ يبقى أحمر دائماً
)
```

---

## 🔐 اختبار المصادقة

### 1️⃣ تسجيل الخروج
**الخطوات:**
1. اضغط على "Log out"

**النتائج المتوقعة:**
- ✅ تسجيل خروج فوري
- ✅ الانتقال إلى شاشة Login
- ✅ Backstack يتم تنظيفه

**الكود:**
```kotlin
Card(
    modifier = Modifier.clickable {
        viewModel.signOut()  // ✅ تسجيل الخروج
        navController.navigate(Screen.Login.route) {
            popUpTo(Screen.Home.route) { inclusive = true }  // ✅ تنظيف الـ Backstack
        }
    }
)
```

### 2️⃣ عدم الوصول للهوم بدون تسجيل دخول
**الخطوات:**
1. سجّل الخروج من الهوم
2. حاول الانتقال المباشر للهوم (إذا أمكن)

**النتائج المتوقعة:**
- ✅ إعادة التوجيه إلى Login
- ✅ عدم الوصول للهوم بدون تسجيل دخول

### 3️⃣ عرض بيانات المستخدم (اختياري)
**الخطوات:**
1. لاحظ أن الحساب الحالي يظهر

**النتائج المتوقعة:**
- ✅ بيانات المستخدم صحيحة
- ✅ يظهر اسم المستخدم أو البريد

---

## ⚡ اختبارات الأداء

### 1️⃣ سرعة التحميل
**الأداة:** Android Profiler

**الخطوات:**
1. افتح Android Studio
2. اذهب إلى View → Tool Windows → Profiler
3. شغّل التطبيق
4. راقب Memory و CPU

**النتائج المتوقعة:**
- ✅ Memory: < 100MB
- ✅ CPU: < 5% في حالة التوقف
- ✅ Frames: 60 FPS

**الأوامر:**
```bash
# فتح Profiler
./gradlew profileDebugVariant

# قياس الأداء
adb shell am trace-ipc start
adb shell am trace-ipc stop
```

### 2️⃣ استجابة الضغط
**الخطوات:**
1. اضغط على العناصر المختلفة
2. لاحظ الاستجابة الفورية

**النتائج المتوقعة:**
- ✅ استجابة فورية (< 100ms)
- ✅ بدون تأخير ملحوظ

### 3️⃣ تقليل الإعادات
**الخطوات:**
1. أضف Log statements
2. لاحظ عدد مرات recomposition

```kotlin
@Composable
fun HomeScreenNew(...) {
    Log.d("HomeScreen", "Recompose called")  // ✅ قياس الإعادات
    
    // ...
}
```

**النتائج المتوقعة:**
- ✅ Recomposition < 5 مرات عند الفتح
- ✅ بدون recomposition إضافية غير ضرورية

---

## 🔍 اختبارات التوافقية

### 1️⃣ أحجام الشاشات المختلفة
**الأجهزة:**
- Phone (4.5 - 6.5 inches)
- Tablet (7 - 10 inches)
- Foldable

**النتائج المتوقعة:**
- ✅ تكيف صحيح على جميع الأحجام
- ✅ لا توجد عناصر مقطوعة
- ✅ الشريط السفلي مرئي

### 2️⃣ الاتجاهات
**الخطوات:**
1. دوّر الجهاز بين Portrait و Landscape

**النتائج المتوقعة:**
- ✅ إعادة تكيف صحيحة
- ✅ حفظ الحالة (Dark Mode)
- ✅ لا توجد مشاكل في التنسيق

### 3️⃣ الإصدارات المختلفة
**الإصدارات:**
- API 28 (Android 9.0)
- API 30 (Android 11.0)
- API 34 (Android 14.0)
- API 35 (Android 15.0)

**النتائج المتوقعة:**
- ✅ عمل سلس على جميع الإصدارات

---

## 📝 نموذج اختبار الانحدار

### قائمة التحقق

- [ ] الصفحة تفتح بنجاح
- [ ] جميع العناصر تظهر
- [ ] Dark Mode يعمل
- [ ] التنقل يعمل بشكل صحيح
- [ ] تسجيل الخروج يعمل
- [ ] الأداء ممتازة
- [ ] التوافقية على أحجام مختلفة
- [ ] لا توجد أخطاء في Logcat
- [ ] لا توجد تسريبات ذاكرة
- [ ] الانتقالات سلسة

---

## 🐛 استكشاف الأخطاء

### مشكلة: الصفحة لا تفتح
**الحل:**
```bash
# تحقق من الأخطاء
adb logcat | grep HomeScreen

# أعد بناء المشروع
./gradlew clean build

# نظف الـ cache
rm -rf .gradle/
```

### مشكلة: Dark Mode لا يعمل
**السبب المحتمل:**
```kotlin
// تحقق من SettingsViewModel
val settingsState by settingsViewModel.uiState.collectAsState()
// تأكد أن isDarkMode موجود
```

**الحل:**
```kotlin
// تحقق من إعدادات الثيم
HandSpeakTheme(darkTheme = settingsState.isDarkMode)
```

### مشكلة: أخطاء Navigation
**السبب المحتمل:**
```kotlin
// المسار ناقص في Screen.kt
object History : Screen("history")  // تأكد أن هذا موجود
```

**الحل:**
```bash
# أضف الخط المفقود في Screen.kt
# ثم أعد البناء
./gradlew clean build
```

---

## ✅ قائمة الانتهاء

عند الانتهاء من جميع الاختبارات:

- [ ] جميع الاختبارات الأساسية ✅
- [ ] جميع اختبارات التنقل ✅
- [ ] جميع اختبارات Dark Mode ✅
- [ ] جميع اختبارات المصادقة ✅
- [ ] جميع اختبارات الأداء ✅
- [ ] جميع اختبارات التوافقية ✅
- [ ] لا توجد أخطاء أو مشاكل
- [ ] التطبيق جاهز للإصدار

---

## 📞 الدعم والمساعدة

إذا واجهت أي مشاكل:

1. تحقق من الأخطاء في Logcat
2. استخدم Android Profiler لقياس الأداء
3. أعد بناء المشروع (Clean build)
4. امسح بيانات التطبيق وأعد تشغيله

---

**تم إعداد دليل الاختبار: ديسمبر 2025** ✨
