# 📥 دليل تحميل وتخزين الصور في HandSpeak

**تاريخ التحديث**: نوفمبر 2025

---

## ✅ الميزات الجديدة

### 1. تحميل الصور من الإنترنت
- ✅ تحميل الصور من أي URL
- ✅ حفظها محلياً في Internal Storage
- ✅ إدارة تلقائية لحجم Cache (50 MB)

### 2. نظام التخزين الذكي
- ✅ أولوية للصور المحمّلة (Storage) على الصور المدمجة (Assets)
- ✅ البحث التلقائي في كلا المكانين
- ✅ دعم كامل للصور الموجودة في Assets

### 3. واجهة المستخدم
- ✅ شاشة تحميل الصور في الإعدادات
- ✅ عرض حجم Cache
- ✅ إمكانية تنظيف Cache

---

## 📋 كيفية الاستخدام

### 1. الوصول إلى شاشة التحميل

1. افتح التطبيق
2. اذهب إلى **الإعدادات** (Settings)
3. اضغط على **"تحميل الصور"** في قسم الصور

---

### 2. تحميل صورة جديدة

#### الخطوات:
1. **أدخل رابط الصورة (URL)**
   - مثال: `https://example.com/signs/alef/1.png`

2. **أدخل اسم المجلد**
   - استخدم أسماء من `sign_map.json`
   - أمثلة: `alef`, `marhaba`, `baa`, `shokran`

3. **أدخل رقم الصورة**
   - ابدأ من `1` ثم `2`, `3`, ...
   - يجب أن تكون متسلسلة

4. **اضغط "تحميل الصورة"**
   - انتظر حتى يكتمل التحميل
   - ستظهر رسالة نجاح أو خطأ

---

### 3. إدارة الصور المحفوظة

#### عرض حجم Cache:
- في شاشة التحميل، ستجد **"معلومات التخزين"**
- يعرض حجم جميع الصور المحفوظة بالـ MB

#### تنظيف Cache:
- اضغط على **"تنظيف Cache"**
- سيتم حذف جميع الصور المحمّلة

---

## 🔧 كيف يعمل النظام

### أولوية البحث:

```
1. Storage المحلي (الصور المحمّلة)
   ↓ (إذا لم توجد)
2. Assets (الصور المدمجة)
```

### مثال:

```kotlin
// البحث عن صورة "alef/1.png"
ImageHelper.getImagePath(context, "alef", 1)

// النظام يبحث:
// 1. /data/data/com.example.handspeak/files/sign_images/alef/1.png
// 2. assets/signs/alef/1.png
```

---

## 📁 هيكل التخزين

### Internal Storage:
```
/data/data/com.example.handspeak/files/
└── sign_images/
    ├── alef/
    │   ├── 1.png
    │   ├── 2.png
    │   └── ...
    ├── marhaba/
    │   └── ...
    └── ...
```

### Assets (المدمجة):
```
app/src/main/assets/
└── signs/
    ├── alef/
    │   ├── 1.png
    │   └── ...
    └── ...
```

---

## 💡 نصائح مهمة

### 1. أسماء المجلدات
- استخدم الأسماء من `sign_map.json`
- يجب أن تطابق تماماً (حساسة لحالة الأحرف)

### 2. تنسيق الصور
- **PNG** (مُفضّل)
- **JPG/JPEG** (مدعوم)
- **حجم**: 512×512 أو 1024×1024 بكسل

### 3. ترتيب الصور
- ابدأ من `1.png`
- استمر بشكل متسلسل: `2.png`, `3.png`, ...
- لا تترك فجوات في الأرقام

### 4. حجم Cache
- **الحد الأقصى**: 50 MB
- عند تجاوز الحد، يتم تنظيف Cache تلقائياً

---

## 🔄 التكامل مع التطبيق

### في TextToSignScreen:
```kotlin
// النظام يبحث تلقائياً في:
// 1. Storage المحلي
// 2. Assets
val imagePath = ImageHelper.getImagePath(context, folder, index)
```

### في ImageHelper:
```kotlin
// البحث الذكي
fun getImageCount(context: Context, folder: String): Int {
    // أولاً: Storage المحلي
    val localCount = ImageDownloader.getImageCountInStorage(context, folder)
    if (localCount > 0) return localCount
    
    // ثانياً: Assets
    // ...
}
```

---

## 🛠️ API المتاحة

### ImageDownloader:

```kotlin
// تحميل صورة
suspend fun downloadImage(
    context: Context,
    imageUrl: String,
    folder: String,
    index: Int
): Bitmap?

// تحميل من Storage
fun loadImageFromStorage(
    context: Context,
    folder: String,
    index: Int
): Bitmap?

// التحقق من الوجود
fun imageExistsInStorage(
    context: Context,
    folder: String,
    index: Int
): Boolean

// عدد الصور
fun getImageCountInStorage(
    context: Context,
    folder: String
): Int

// حذف صورة
fun deleteImage(
    context: Context,
    folder: String,
    index: Int
): Boolean

// تنظيف Cache
fun clearAllImages(context: Context): Boolean

// حجم Cache
fun getTotalCacheSize(context: Context): Long
```

---

## 📝 أمثلة الاستخدام

### مثال 1: تحميل صورة واحدة
```
URL: https://example.com/signs/alef/1.png
المجلد: alef
الرقم: 1
```

### مثال 2: تحميل عدة صور
```
URL: https://example.com/signs/alef/1.png → alef/1
URL: https://example.com/signs/alef/2.png → alef/2
URL: https://example.com/signs/alef/3.png → alef/3
```

### مثال 3: استبدال صورة موجودة
```
- إذا كانت الصورة موجودة في Storage، سيتم استبدالها
- إذا كانت في Assets فقط، سيتم إضافة نسخة في Storage
```

---

## ⚠️ ملاحظات مهمة

### 1. الصلاحيات
- ✅ `INTERNET` - موجود في AndroidManifest
- ✅ Internal Storage - لا يحتاج صلاحيات

### 2. الأمان
- الصور تُحفظ في Internal Storage (خاصة بالتطبيق)
- لا يمكن الوصول إليها من تطبيقات أخرى

### 3. الأداء
- التحميل يتم في background thread
- لا يؤثر على UI
- Cache management تلقائي

---

## ✅ الخلاصة

### الميزات:
1. ✅ تحميل الصور من الإنترنت
2. ✅ حفظ محلي في Internal Storage
3. ✅ أولوية للصور المحمّلة
4. ✅ إدارة Cache تلقائية
5. ✅ واجهة مستخدم سهلة

### الفوائد:
- 🚀 **مرونة**: إضافة صور جديدة بدون تحديث التطبيق
- 💾 **توفير المساحة**: Cache management ذكي
- 🔄 **تكامل سلس**: يعمل مع النظام الحالي تلقائياً

---

**آخر تحديث**: نوفمبر 2025  
**الحالة**: ✅ **جاهز للاستخدام**

