# 🚀 البدء السريع - HandSpeak

دليل سريع للبدء مع تطبيق HandSpeak في 5 دقائق!

## ⚡ خطوات سريعة

### 1. Clone المشروع (30 ثانية)

```bash
git clone https://github.com/yourusername/HandSpeak.git
cd HandSpeak
```

### 2. تحميل النماذج (2-3 دقائق)

#### أ. نموذج MediaPipe (ضروري)
```bash
# تحميل نموذج MediaPipe
cd app/src/main/assets
wget https://storage.googleapis.com/mediapipe-models/hand_landmarker/hand_landmarker/float16/latest/hand_landmarker.task
cd ../../../..
```

**أو باستخدام المتصفح:**
1. افتح: https://storage.googleapis.com/mediapipe-models/hand_landmarker/hand_landmarker/float16/latest/hand_landmarker.task
2. احفظ الملف
3. انسخه إلى: `app/src/main/assets/`

#### ب. نموذج TensorFlow Lite (اختياري للاختبار)
إذا لم يكن لديك نموذج مدرب، يمكنك:
- استخدام نموذج تجريبي بسيط
- أو تخطي ميزة "إشارة → نص" مؤقتاً والعمل على "نص → إشارة" فقط

### 3. إضافة صور الإشارات (اختياري، 1-2 دقيقة)

إنشاء بعض المجلدات التجريبية:

```bash
mkdir -p app/src/main/assets/signs/alef
mkdir -p app/src/main/assets/signs/baa
mkdir -p app/src/main/assets/signs/marhaba
```

**ملاحظة:** يمكنك استخدام أي صور مؤقتة للاختبار أو تخطي هذه الخطوة.

### 4. فتح المشروع في Android Studio (30 ثانية)

```bash
# في مجلد المشروع
studio .
# أو افتح Android Studio واختر "Open" ثم حدد مجلد المشروع
```

### 5. Sync و Build (1-2 دقيقة)

عند فتح المشروع، Android Studio سيقوم تلقائياً بـ:
- Gradle Sync
- تحميل المكتبات

**إذا لم يحدث تلقائياً:**
- File → Sync Project with Gradle Files
- Build → Rebuild Project

### 6. تشغيل التطبيق (30 ثانية)

1. وصّل هاتف Android أو شغّل Emulator
2. اضغط على زر "Run" (▶️) في Android Studio
3. التطبيق سيُثبّت ويشتغل تلقائياً

## 📱 الاختبار السريع

### اختبار 1: الشاشة الرئيسية
- التطبيق يفتح ويعرض 5 بطاقات
- جميع الأزرار تعمل
- ✅ نجح!

### اختبار 2: نص → إشارة (لا يحتاج نماذج)
1. اضغط على "نص إلى إشارة"
2. اكتب "مرحبا"
3. اضغط "ترجم"
4. ✅ يجب أن تظهر بطاقة الإشارة (مع placeholder icon)

### اختبار 3: الإعدادات
1. اضغط على "الإعدادات"
2. جرّب تغيير الإعدادات
3. ✅ جميع الإعدادات تعمل

### اختبار 4: إشارة → نص (يحتاج النماذج)
1. اضغط على "إشارة إلى نص"
2. امنح إذن الكاميرا
3. وجّه الكاميرا نحو يدك
4. ✅ يجب أن تظهر الكاميرا (حتى بدون نموذج TFLite)

## 🔧 حل المشاكل السريع

### مشكلة: Gradle Sync فشل
```bash
# حل 1: استخدم Java 11
# تحقق من File → Project Structure → SDK Location

# حل 2: نظّف المشروع
./gradlew clean

# حل 3: أعد تحميل المكتبات
./gradlew --refresh-dependencies
```

### مشكلة: التطبيق لا يُبنى
```bash
# تحقق من ملف gradle.properties
# تأكد من وجود:
android.useAndroidX=true
android.enableJetifier=true
```

### مشكلة: خطأ في MediaPipe
```
الحل:
1. تأكد من تحميل hand_landmarker.task
2. تأكد من وجود الملف في app/src/main/assets/
3. اسم الملف يجب أن يكون بالضبط: hand_landmarker.task
```

### مشكلة: الكاميرا لا تعمل في Emulator
```
الحل:
استخدم جهاز حقيقي أو:
1. في AVD Manager
2. Edit Virtual Device
3. Show Advanced Settings
4. Camera → Webcam
```

## 📋 الميزات المتاحة بدون النماذج الكاملة

حتى بدون نموذج TFLite المدرب، يمكنك اختبار:

✅ **تعمل بالكامل:**
- الشاشة الرئيسية
- نص → إشارة (عرض placeholder)
- صوت → إشارة (مع التعرف على الصوت)
- السجل (حفظ وعرض)
- الإعدادات (جميع الخيارات)

⚠️ **تعمل جزئياً:**
- إشارة → نص (الكاميرا تعمل، لكن بدون ترجمة حقيقية)

## 🎯 الخطوات التالية

بعد التشغيل الناجح:

### للمستخدمين:
1. أضف نموذج TFLite المدرب لترجمة فعلية
2. أضف صور الإشارات الحقيقية
3. اختبر جميع الميزات

### للمطورين:
1. اقرأ [README.md](README.md) للفهم الكامل
2. راجع [CONTRIBUTING.md](CONTRIBUTING.md) للمساهمة
3. اتبع [SETUP_MODELS.md](SETUP_MODELS.md) لإعداد النماذج

## 📚 ملفات مهمة

| الملف | الوصف |
|-------|-------|
| [README.md](README.md) | دليل شامل للمشروع |
| [SETUP_MODELS.md](SETUP_MODELS.md) | كيفية إعداد النماذج |
| [CONTRIBUTING.md](CONTRIBUTING.md) | دليل المساهمة |
| [app/build.gradle.kts](app/build.gradle.kts) | إعدادات المشروع |

## 💡 نصائح سريعة

### نصيحة 1: اختبر على جهاز حقيقي
Emulator بطيء مع الكاميرا والـ ML. استخدم جهاز حقيقي للحصول على تجربة أفضل.

### نصيحة 2: استخدم Debug Build
```bash
# للتطوير والاختبار
./gradlew installDebug

# للإنتاج فقط
./gradlew assembleRelease
```

### نصيحة 3: راقب Logs
```bash
# فلتر logs التطبيق
adb logcat | grep -E "HandSpeak|SignLanguage|MediaPipe"
```

### نصيحة 4: Clean عند الحاجة
```bash
# إذا واجهت مشاكل غريبة
./gradlew clean
# ثم
./gradlew assembleDebug
```

## 🎉 مبروك!

إذا وصلت هنا والتطبيق يعمل، فأنت الآن جاهز لاستكشاف HandSpeak!

### الخطوة التالية:
- جرّب جميع الميزات
- اقرأ الكود المصدري
- ساهم في التطوير
- شارك التطبيق مع الآخرين

## 🆘 تحتاج مساعدة؟

- **مشاكل تقنية:** افتح [Issue](https://github.com/yourusername/HandSpeak/issues)
- **أسئلة عامة:** استخدم [Discussions](https://github.com/yourusername/HandSpeak/discussions)
- **البريد الإلكتروني:** handspeak@example.com

---

**وقت القراءة:** 3 دقائق  
**وقت التطبيق:** 5-10 دقائق  
**الصعوبة:** سهل ⭐

صُنع بـ ❤️ لمجتمع المطورين العرب











