# 🧠 دليل التعلم التكيفي - HandSpeak

**تاريخ التحديث**: نوفمبر 2025

---

## ✅ ما تم إضافته

### نظام التعلم التكيفي (Adaptive Learning)

التطبيق الآن **يتعلم من استخدامك** تلقائياً!

---

## 🎯 كيف يعمل

### العملية التلقائية:

```
1. 📸 المستخدم يستخدم التطبيق
   ↓
2. 🤖 التطبيق يتعرف على الإشارة
   ↓
3. ✅ إذا كانت الثقة ≥70%
   ↓
4. 💾 حفظ العينة تلقائياً (landmarks + label)
   ↓
5. 📊 البيانات تُجمع في CSV
   ↓
6. 🔄 يمكن استخدامها لإعادة التدريب
```

---

## 🔧 المكونات المضافة

### 1. AdaptiveLearningHelper.kt ✅

**الوظائف:**
- ✅ `saveTrainingSample()` - حفظ عينة تدريب
- ✅ `getLearningStats()` - إحصائيات التعلم
- ✅ `getTotalSampleCount()` - عدد العينات
- ✅ `clearTrainingData()` - حذف البيانات

### 2. تحديث SignToTextViewModel.kt ✅

**الميزة:**
- ✅ حفظ تلقائي عند التعرف (إذا كانت الثقة ≥70%)
- ✅ يمكن تفعيل/تعطيل من الإعدادات

### 3. شاشة إحصائيات التعلم ✅

**الميزات:**
- ✅ عرض إجمالي العينات
- ✅ عرض العينات حسب التصنيف
- ✅ إمكانية حذف البيانات

---

## 🚀 كيفية الاستخدام

### 1. تفعيل التعلم التكيفي:

```
الإعدادات → التعلم التكيفي → تفعيل التعلم التكيفي
```

### 2. استخدام التطبيق:

- استخدم **"إشارة إلى نص"** بشكل طبيعي
- التطبيق يحفظ العينات تلقائياً
- فقط العينات عالية الثقة (≥70%)

### 3. عرض الإحصائيات:

```
الإعدادات → التعلم التكيفي → إحصائيات التعلم
```

---

## 📊 البيانات المحفوظة

### التنسيق (CSV):

```csv
label,x0,y0,z0,x1,y1,z1,...,x20,y20,z20,timestamp
أ,0.5,0.3,0.1,0.6,0.4,0.2,...,2025-11-14 10:30:00
مرحبا,0.4,0.2,0.0,0.5,0.3,0.1,...,2025-11-14 10:31:00
```

### الموقع:

```
/data/data/com.example.handspeak/files/training_data/user_training_data.csv
```

---

## 💡 الميزات

### ✅ تلقائي بالكامل
- لا يحتاج تدخل من المستخدم
- يحفظ تلقائياً عند التعرف

### ✅ ذكي
- يحفظ فقط العينات عالية الثقة (≥70%)
- حد أقصى: 100 عينة لكل تصنيف

### ✅ آمن
- البيانات محلية فقط
- لا تُرسل لأي مكان

---

## 🔄 استخدام البيانات للتدريب

### الخطوات:

1. **تصدير البيانات:**
   ```bash
   # من التطبيق
   adb pull /data/data/com.example.handspeak/files/training_data/user_training_data.csv .
   ```

2. **استخدام في Colab:**
   ```python
   import pandas as pd
   
   # قراءة البيانات
   df = pd.read_csv('user_training_data.csv')
   
   # استخدامها للتدريب
   X = df.iloc[:, 1:-1].values  # Features (63 columns)
   y = df['label'].values        # Labels
   ```

3. **إعادة تدريب النموذج:**
   ```python
   # دمج مع البيانات الأصلية
   X_combined = np.vstack([X_original, X_user])
   y_combined = np.hstack([y_original, y_user])
   
   # تدريب
   model.fit(X_combined, y_combined, ...)
   ```

4. **تحديث النموذج:**
   ```bash
   # نسخ النموذج الجديد
   cp arabic_sign_lstm.tflite app/src/main/assets/
   ```

---

## 📋 الإعدادات

### في SettingsScreen:

1. **تفعيل التعلم التكيفي:**
   - ✅ مفعّل افتراضياً
   - يمكن تعطيله من الإعدادات

2. **إحصائيات التعلم:**
   - عرض عدد العينات
   - عرض حسب التصنيف
   - إمكانية الحذف

---

## ✅ الخلاصة

### ما تم إضافته:
1. ✅ نظام جمع البيانات التلقائي
2. ✅ حفظ في CSV
3. ✅ إحصائيات التعلم
4. ✅ إعدادات التحكم

### الفوائد:
- 🎯 **دقة أفضل** - التعلم من الاستخدام الفعلي
- 📊 **بيانات حقيقية** - من المستخدمين الحقيقيين
- 🔄 **قابل للتحديث** - يمكن إعادة التدريب

---

**آخر تحديث**: نوفمبر 2025  
**الحالة**: ✅ **جاهز للاستخدام**










