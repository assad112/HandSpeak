# 📜 سكريبتات HandSpeak

هذا المجلد يحتوي على سكريبتات مساعدة لإعداد المشروع.

## 🐍 create_dummy_model.py

سكريبت Python لإنشاء نموذج TFLite تجريبي للاختبار.

### المتطلبات:
```bash
pip install tensorflow
```

### الاستخدام:
```bash
cd scripts
python create_dummy_model.py
```

### الناتج:
- `arabic_sign_lstm.tflite` - نموذج تجريبي للاختبار

### ملاحظات:
- ⚠️ هذا النموذج **تجريبي فقط** ولن يعطي نتائج دقيقة
- للحصول على نتائج دقيقة، قم بتدريب النموذج على البيانات الفعلية
- راجع `SETUP_MODELS.md` لتدريب النموذج الحقيقي

### الخطوات بعد الإنشاء:
1. انسخ الملف إلى `app/src/main/assets/arabic_sign_lstm.tflite`
2. قم بـ Clean و Rebuild للمشروع
3. اختبر التطبيق

---

**ملاحظة**: تأكد من وجود Python 3.7+ و TensorFlow 2.x

