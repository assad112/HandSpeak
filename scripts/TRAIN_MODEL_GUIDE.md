# 🚀 دليل تدريب النموذج على الإشارات الجديدة

## 📋 نظرة عامة

هذا الدليل يشرح كيفية إنشاء نموذج مدرب على الحروف العربية والإشارات الجديدة.

## ✅ المتطلبات

```bash
pip install tensorflow numpy
```

## 🔧 الطريقة 1: إنشاء نموذج تجريبي (للاختبار)

### الخطوات:

1. **تنفيذ السكريبت:**
```bash
cd scripts
python train_model_with_new_signs.py
```

2. **النتيجة:**
   - سيتم إنشاء ملف `arabic_sign_lstm.tflite` في مجلد `scripts/`
   - النموذج يدعم 48 تصنيف (28 حرف + 20 إشارة جديدة)

3. **نسخ النموذج إلى المشروع:**
```bash
# من مجلد scripts
cp arabic_sign_lstm.tflite ../app/src/main/assets/
```

4. **إعادة بناء التطبيق:**
   - في Android Studio: `Build > Clean Project`
   - ثم `Build > Rebuild Project`

### ⚠️ ملاحظة مهمة:
هذا النموذج **تجريبي فقط** ولن يعطي نتائج دقيقة. للاستخدام الفعلي، يجب تدريبه على بيانات حقيقية.

---

## 🎯 الطريقة 2: تدريب نموذج حقيقي (موصى به)

### المتطلبات:
- بيانات تدريب (صور أو فيديوهات للإشارات)
- Google Colab (للتدريب المجاني)

### الخطوات:

#### 1. إعداد البيانات

قم بتنظيم بياناتك بهذا الشكل:
```
dataset/
├── أ/
│   ├── image1.jpg
│   ├── image2.jpg
│   └── ...
├── ب/
│   ├── image1.jpg
│   └── ...
├── أحبك/
│   ├── image1.jpg
│   └── ...
└── ...
```

#### 2. استخدام Google Colab

1. افتح ملف `app/colab/ArabicSignTraining.ipynb` في Google Colab
2. ارفع بياناتك إلى Google Drive
3. عدّل الكود ليشير إلى بياناتك
4. شغّل الخلايا بالترتيب

#### 3. تحميل النموذج

بعد التدريب، سيتم تحميل:
- `arabic_sign_lstm.tflite`
- `labels.json`

انسخهما إلى:
- `app/src/main/assets/arabic_sign_lstm.tflite`
- `app/src/main/assets/labels.json`

---

## 📊 الإشارات المدعومة حالياً

### الحروف (28):
أ، ب، ت، ث، ج، ح، خ، د، ذ، ر، ز، س، ش، ص، ض، ط، ظ، ع، غ، ف، ق، ك، ل، م، ن، هـ، و، ي

### الإشارات الجديدة (20):
- أحبك
- حقاً_أحبك
- أراك_لاحقاً
- أضحكتني
- سؤال
- اقتباس
- هذا_رهيب
- أنا_أراقبك
- عمل_جيد
- أتمنى_لك_حياة_سعيدة
- موافق
- أنت
- لا
- هذا_ممتاز
- لست_متأكد
- مرحباً
- شجرة
- بطة
- قطة
- هاتف

---

## 🔍 التحقق من النموذج

بعد نسخ النموذج إلى `app/src/main/assets/`:

1. تأكد من أن `labels.json` يحتوي على جميع التصنيفات بالترتيب الصحيح
2. تأكد من أن عدد التصنيفات في `labels.json` يطابق عدد التصنيفات في النموذج
3. أعد بناء التطبيق
4. اختبر الكاميرا في التطبيق

---

## 🐛 حل المشاكل

### المشكلة: النموذج لا يتعرف على الإشارات
- **الحل**: تأكد من أن النموذج مدرب على بيانات حقيقية
- النموذج التجريبي لن يعطي نتائج دقيقة

### المشكلة: خطأ في عدد التصنيفات
- **الحل**: تأكد من أن `labels.json` يحتوي على نفس عدد التصنيفات التي درّب عليها النموذج

### المشكلة: النموذج كبير جداً
- **الحل**: استخدم `converter.optimizations = [tf.lite.Optimize.DEFAULT]` لضغط النموذج

---

## 📚 مراجع إضافية

- `SETUP_MODELS.md` - دليل شامل لإعداد النماذج
- `HOW_TO_ADD_AI.md` - كيفية إضافة الذكاء الاصطناعي
- `app/colab/ArabicSignTraining.ipynb` - سكريبت التدريب في Colab

