# ⚡ بدء سريع - إنشاء نموذج تجريبي

## 📦 تثبيت المتطلبات

```bash
pip install -r requirements.txt
```

أو مباشرة:
```bash
pip install tensorflow numpy
```

## 🚀 تشغيل السكريبت

```bash
cd scripts
python train_model_with_new_signs.py
```

## 📁 النتيجة

سيتم إنشاء ملف `arabic_sign_lstm.tflite` في مجلد `scripts/`

## 📋 الخطوات التالية

1. **انسخ النموذج:**
```bash
# من مجلد scripts
copy arabic_sign_lstm.tflite ..\app\src\main\assets\
```

2. **في Android Studio:**
   - `Build > Clean Project`
   - `Build > Rebuild Project`

3. **اختبر التطبيق:**
   - افتح شاشة الكاميرا
   - جرب الإشارات

## ⚠️ ملاحظة

هذا نموذج **تجريبي** للاختبار فقط. للاستخدام الفعلي، يجب تدريبه على بيانات حقيقية.

راجع `TRAIN_MODEL_GUIDE.md` للتفاصيل الكاملة.

