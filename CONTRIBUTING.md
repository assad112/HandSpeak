# المساهمة في HandSpeak 🤝

شكراً لاهتمامك بالمساهمة في HandSpeak! نحن نرحب بجميع المساهمات من المطورين والمصممين وخبراء لغة الإشارة.

## 📋 جدول المحتويات

- [كيفية المساهمة](#كيفية-المساهمة)
- [أنواع المساهمات](#أنواع-المساهمات)
- [معايير الكود](#معايير-الكود)
- [عملية Pull Request](#عملية-pull-request)
- [الإبلاغ عن الأخطاء](#الإبلاغ-عن-الأخطاء)
- [طلب ميزات جديدة](#طلب-ميزات-جديدة)

## 🚀 كيفية المساهمة

### 1. Fork المشروع

انقر على زر "Fork" في أعلى الصفحة لإنشاء نسخة من المشروع في حسابك.

### 2. Clone المشروع

```bash
git clone https://github.com/your-username/HandSpeak.git
cd HandSpeak
```

### 3. إنشاء Branch جديد

```bash
git checkout -b feature/AmazingFeature
```

**تسمية الـ Branches:**
- `feature/feature-name` - للميزات الجديدة
- `fix/bug-description` - لإصلاح الأخطاء
- `docs/update-description` - لتحديث التوثيق
- `refactor/component-name` - لإعادة الهيكلة
- `test/test-description` - لإضافة الاختبارات

### 4. إجراء التغييرات

قم بإضافة أو تعديل الملفات المطلوبة مع الالتزام بمعايير الكود.

### 5. Commit التغييرات

```bash
git add .
git commit -m "Add: وصف موجز للتغييرات"
```

**معايير رسائل الـ Commit:**
- `Add:` - إضافة ميزة أو ملف جديد
- `Fix:` - إصلاح خطأ
- `Update:` - تحديث كود موجود
- `Refactor:` - إعادة هيكلة الكود
- `Docs:` - تحديث التوثيق
- `Test:` - إضافة أو تحديث الاختبارات
- `Style:` - تغييرات تنسيقية فقط

### 6. Push إلى GitHub

```bash
git push origin feature/AmazingFeature
```

### 7. فتح Pull Request

افتح Pull Request من branch الخاص بك إلى `main` في المستودع الأصلي.

## 🎨 أنواع المساهمات

### 1. تحسين الكود

- تحسين الأداء
- تقليل استهلاك الذاكرة
- تحسين دقة التعرف
- إضافة Unit Tests

### 2. إضافة ميزات جديدة

- دعم إشارات جديدة
- تحسين واجهة المستخدم
- إضافة animations جديدة
- دعم فيديوهات الإشارات

### 3. إصلاح الأخطاء

- إصلاح crashes
- حل مشاكل الأداء
- إصلاح أخطاء الترجمة
- معالجة edge cases

### 4. تحسين التوثيق

- إضافة أمثلة
- تحسين README
- كتابة دروس تعليمية
- ترجمة التوثيق

### 5. تحسين التصميم

- تحسين UI/UX
- إضافة themes جديدة
- تحسين Accessibility
- إضافة animations

### 6. البيانات والنماذج

- إضافة صور إشارات جديدة
- تحسين جودة الصور
- تدريب نماذج محسّنة
- توسيع dataset

## 💻 معايير الكود

### Kotlin Style Guide

اتبع [Kotlin Coding Conventions](https://kotlinlang.org/docs/coding-conventions.html):

```kotlin
// ✅ جيد
class SignLanguageClassifier(private val context: Context) {
    private var interpreter: Interpreter? = null
    
    fun classify(landmarks: FloatArray): Pair<String, Float>? {
        // Implementation
    }
}

// ❌ سيء
class sign_language_classifier(context: Context) {
    var interpreter: Interpreter? = null
    
    fun Classify(Landmarks: FloatArray): Pair<String, Float>? {
        // Implementation
    }
}
```

### Compose Best Practices

```kotlin
// ✅ جيد - دالة Composable منفصلة
@Composable
fun SignCard(
    sign: SignInfo,
    onClick: () -> Unit,
    modifier: Modifier = Modifier
) {
    Card(
        onClick = onClick,
        modifier = modifier
    ) {
        // Content
    }
}

// ❌ سيء - كود مكرر داخل Composable
@Composable
fun Screen() {
    Column {
        Card { /* ... */ }
        Card { /* ... */ }
        Card { /* ... */ }
    }
}
```

### معايير التسمية

- **Classes**: `PascalCase` - `SignLanguageClassifier`
- **Functions**: `camelCase` - `processHandLandmarks()`
- **Variables**: `camelCase` - `detectedText`
- **Constants**: `UPPER_SNAKE_CASE` - `MAX_CONFIDENCE`
- **Composables**: `PascalCase` - `HomeScreen()`

### التعليقات

```kotlin
/**
 * يصنف معالم اليد إلى إشارة عربية.
 *
 * @param landmarks مصفوفة من 63 قيمة (21 landmark × 3 coordinates)
 * @return زوج من (نص الإشارة، نسبة الثقة) أو null إذا فشل التصنيف
 */
fun classify(landmarks: FloatArray): Pair<String, Float>? {
    // Implementation
}
```

## 📝 عملية Pull Request

### قبل إنشاء PR

- [ ] تأكد من أن الكود يعمل بدون أخطاء
- [ ] قم بتشغيل جميع الاختبارات
- [ ] تحقق من عدم وجود Lint Errors
- [ ] أضف تعليقات للكود المعقد
- [ ] حدّث التوثيق إذا لزم الأمر

### وصف PR

استخدم القالب التالي:

```markdown
## الوصف
وصف موجز للتغييرات المضافة.

## نوع التغيير
- [ ] إصلاح خطأ (Bug fix)
- [ ] ميزة جديدة (New feature)
- [ ] تغيير كبير (Breaking change)
- [ ] تحديث التوثيق (Documentation update)

## كيفية الاختبار
خطوات اختبار التغييرات:
1. افتح التطبيق
2. انتقل إلى...
3. تحقق من...

## Screenshots (إن وجدت)
أضف صور للتغييرات في الواجهة.

## Checklist
- [ ] الكود يتبع معايير المشروع
- [ ] تم إضافة تعليقات للكود المعقد
- [ ] تم تحديث التوثيق
- [ ] لا توجد lint errors جديدة
- [ ] تم اختبار التغييرات
```

### مراجعة الكود

- سيقوم المشرفون بمراجعة PR الخاص بك
- قد يطلبون تغييرات أو تحسينات
- قم بالرد على التعليقات وإجراء التعديلات المطلوبة
- بعد الموافقة، سيتم دمج PR

## 🐛 الإبلاغ عن الأخطاء

عند الإبلاغ عن خطأ، يرجى تضمين:

### معلومات الجهاز
- نوع الجهاز (مثل: Samsung Galaxy S21)
- إصدار Android (مثل: Android 13)
- إصدار التطبيق

### وصف الخطأ
- وصف واضح ومفصل للمشكلة
- خطوات إعادة إنتاج المشكلة
- السلوك المتوقع
- السلوك الفعلي
- Screenshots أو فيديوهات إن أمكن

### Logs
```
أرفق logs من logcat إن أمكن:
adb logcat | grep -E "HandSpeak|SignLanguage"
```

### مثال على تقرير خطأ

```markdown
**وصف المشكلة:**
التطبيق يتوقف عن العمل عند محاولة فتح الكاميرا.

**خطوات إعادة الإنتاج:**
1. افتح التطبيق
2. اضغط على "إشارة إلى نص"
3. امنح إذن الكاميرا
4. التطبيق يتوقف

**السلوك المتوقع:**
فتح الكاميرا وعرض preview

**معلومات الجهاز:**
- الجهاز: Pixel 6
- Android: 13
- إصدار التطبيق: 1.0

**Logs:**
```
E/AndroidRuntime: FATAL EXCEPTION: main
E/AndroidRuntime: java.lang.RuntimeException: ...
```
```

## ✨ طلب ميزات جديدة

عند طلب ميزة جديدة:

### وصف الميزة
- ما هي الميزة المطلوبة؟
- لماذا تعتقد أنها مفيدة؟
- كيف يجب أن تعمل؟

### أمثلة
- أضف أمثلة أو mockups إن أمكن
- صِف use cases للميزة

### مثال على طلب ميزة

```markdown
**وصف الميزة:**
إضافة وضع "التعلم" حيث يمكن للمستخدم تعلم الإشارات خطوة بخطوة.

**الفائدة:**
سيساعد المبتدئين في تعلم لغة الإشارة بطريقة تفاعلية.

**كيف يجب أن تعمل:**
1. المستخدم يختار "وضع التعلم" من القائمة
2. يتم عرض إشارة واحدة في كل مرة
3. المستخدم يحاول تقليد الإشارة
4. التطبيق يعطي تقييماً للمحاولة
5. الانتقال للإشارة التالية

**Mockups:**
[أضف صور أو رسومات إن أمكن]
```

## 🧪 الاختبارات

### كتابة Unit Tests

```kotlin
@Test
fun `test hand landmark normalization`() {
    val helper = HandDetectionHelper(context, {}, {})
    val landmarks = listOf(
        HandLandmark(0.5f, 0.5f, 0f),
        // ... المزيد من landmarks
    )
    
    val normalized = helper.normalizeLandmarks(landmarks)
    
    assertEquals(63, normalized.size)
    assertTrue(normalized.all { it in 0f..1f })
}
```

### اختبار UI

```kotlin
@Test
fun homeScreenDisplaysAllOptions() {
    composeTestRule.setContent {
        HomeScreen(rememberNavController())
    }
    
    composeTestRule.onNodeWithText("إشارة إلى نص").assertIsDisplayed()
    composeTestRule.onNodeWithText("نص إلى إشارة").assertIsDisplayed()
    // ... المزيد من الاختبارات
}
```

## 📚 موارد مفيدة

- [Kotlin Documentation](https://kotlinlang.org/docs/home.html)
- [Jetpack Compose Docs](https://developer.android.com/jetpack/compose)
- [Material Design 3](https://m3.material.io/)
- [TensorFlow Lite](https://www.tensorflow.org/lite)
- [MediaPipe](https://developers.google.com/mediapipe)

## 💬 التواصل

- **GitHub Issues**: للإبلاغ عن الأخطاء وطلب الميزات
- **GitHub Discussions**: للنقاشات العامة والأسئلة
- **Email**: handspeak@example.com

## 🎖️ المساهمون

شكراً لجميع المساهمين في المشروع!

<!-- ALL-CONTRIBUTORS-LIST:START -->
<!-- سيتم إضافة قائمة المساهمين تلقائياً -->
<!-- ALL-CONTRIBUTORS-LIST:END -->

## 📄 الترخيص

بالمساهمة في هذا المشروع، فإنك توافق على أن مساهماتك ستخضع لنفس ترخيص MIT.

---

شكراً لمساهمتك في جعل HandSpeak أفضل! 🎉











