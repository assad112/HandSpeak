# 🔧 حل مشكلة تثبيت التطبيق - HandSpeak

## ❌ الخطأ: "Requested internal only, but not enough space"

هذا الخطأ يعني أن الجهاز/المحاكي لا يحتوي على مساحة كافية في الذاكرة الداخلية.

---

## ✅ الحلول

### الحل 1: تحرير مساحة على المحاكي (Android Emulator)

#### الطريقة أ: من Android Studio
1. افتح **Device Manager**
2. اختر المحاكي
3. اضغط على **Edit** (أيقونة القلم)
4. اضغط على **Show Advanced Settings**
5. زد **Internal Storage** إلى 4 GB على الأقل
6. اضغط **Finish**
7. احذف المحاكي القديم وأنشئ واحد جديد

#### الطريقة ب: من إعدادات المحاكي
1. شغّل المحاكي
2. اذهب إلى **Settings** → **Storage**
3. احذف التطبيقات غير المستخدمة
4. امسح Cache للتطبيقات الكبيرة

#### الطريقة ج: استخدام ADB
```bash
# تحقق من المساحة المتاحة
adb shell df /data

# احذف التطبيقات غير المستخدمة
adb uninstall <package_name>

# امسح Cache
adb shell pm trim-caches 500M
```

---

### الحل 2: تحرير مساحة على الجهاز الحقيقي

1. **احذف التطبيقات غير المستخدمة**
   - Settings → Apps → احذف التطبيقات الكبيرة

2. **امسح Cache**
   - Settings → Storage → Cache → Clear

3. **احذف الملفات الكبيرة**
   - الصور، الفيديوهات، الملفات المؤقتة

4. **استخدم SD Card** (إن أمكن)
   - انقل بعض التطبيقات إلى SD Card

---

### الحل 3: تقليل حجم APK (مؤقت)

إذا كان APK كبير جداً، يمكن تقليل حجمه:

#### أ. إزالة MediaPipe model مؤقتاً (للاختبار فقط)
```bash
# احذف الملف مؤقتاً للاختبار
# app/src/main/assets/hand_landmarker.task (7.8 MB)
```

#### ب. تفعيل ProGuard/R8
في `app/build.gradle.kts`:
```kotlin
buildTypes {
    getByName("debug") {
        isMinifyEnabled = true
        proguardFiles(
            getDefaultProguardFile("proguard-android-optimize.txt"),
            "proguard-rules.pro"
        )
    }
}
```

---

### الحل 4: استخدام محاكي جديد بمساحة أكبر

1. افتح **Device Manager**
2. اضغط **Create Device**
3. اختر **Phone** → **Pixel 5** (أو أي جهاز)
4. اختر **System Image** → **API 33** (أو أحدث)
5. في **Advanced Settings**:
   - **Internal Storage**: 4 GB على الأقل
   - **SD Card**: 1 GB (اختياري)
6. اضغط **Finish**

---

## 🔍 التحقق من المساحة

### من Android Studio Terminal:
```bash
# تحقق من المساحة على المحاكي
adb shell df /data

# يجب أن ترى مساحة كافية (على الأقل 500 MB)
```

### من الجهاز:
- Settings → Storage
- تحقق من **Available space**

---

## 📊 حجم التطبيق الحالي

| المكون | الحجم |
|--------|-------|
| APK الأساسي | ~15-20 MB |
| hand_landmarker.task | ~7.8 MB |
| **الإجمالي** | **~25-30 MB** |

**المساحة المطلوبة**: على الأقل **100-200 MB** في الذاكرة الداخلية

---

## ✅ الحل السريع (موصى به)

### للمحاكي:
1. **احذف المحاكي الحالي**
2. **أنشئ محاكي جديد** بـ:
   - Internal Storage: **4 GB**
   - RAM: **2 GB** على الأقل
3. **شغّل المحاكي الجديد**
4. **ثبت التطبيق**

### للجهاز الحقيقي:
1. **احذف 3-5 تطبيقات** غير مستخدمة
2. **امسح Cache** لجميع التطبيقات
3. **حاول التثبيت مرة أخرى**

---

## 🎯 الخطوات الموصى بها

### 1. تحرير مساحة (5 دقائق)
```bash
# على المحاكي
adb shell pm list packages | grep -v "android" | head -10
# احذف التطبيقات غير المستخدمة
adb uninstall <package_name>
```

### 2. التحقق من المساحة
```bash
adb shell df /data
# يجب أن ترى على الأقل 500 MB متاح
```

### 3. إعادة المحاولة
```bash
./gradlew installDebug
```

---

## 💡 نصائح إضافية

1. **استخدم محاكي حديث**: API 33+ مع مساحة كافية
2. **احذف المحاكيات القديمة**: لتوفير مساحة على القرص
3. **استخدم AVD Manager**: لإدارة المحاكيات بسهولة
4. **افحص حجم APK**: تأكد أنه معقول (< 50 MB)

---

## 🆘 إذا استمرت المشكلة

1. **أنشئ محاكي جديد تماماً** بمساحة أكبر
2. **استخدم جهاز حقيقي** بدلاً من المحاكي
3. **تحقق من إعدادات Android Studio**: قد تكون هناك مشكلة في الإعدادات

---

## 📞 الدعم

إذا استمرت المشكلة بعد تجربة جميع الحلول:
- راجع [Android Developer Documentation](https://developer.android.com/studio/run/emulator)
- راجع [Troubleshooting Android Emulator](https://developer.android.com/studio/run/emulator-troubleshooting)

---

**آخر تحديث**: نوفمبر 2025

